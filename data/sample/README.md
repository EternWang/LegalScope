# Sample Data

No row-level sample data is included in this public preview.

The previous sample CSVs were removed because they were tied to an outdated workbook
snapshot and stale model-count assumptions. Regenerate sample rows only after privacy,
licensing, and review constraints are cleared.
